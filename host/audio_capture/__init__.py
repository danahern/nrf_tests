"""PSE84 UART audio-capture helpers.

The :mod:`uart_protocol` submodule contains the reusable PCM_BEGIN /
hex / PCM_END parser used by both :mod:`receive_pcm` (the standalone CLI
that writes WAV files) and the assistant bridge's UART transport.
"""
